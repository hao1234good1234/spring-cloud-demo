<?xml version="1.0" encoding="UTF-8"?>
<!-- ⚠️ 关闭自动扫描！避免重载导致转换器丢失 -->
<configuration>

    <!-- 从Spring Environment中读取属性 -->
    <springProperty scope="context" name="LOG_DIR" source="logging.path" defaultValue="./logs"/>
    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="myapp"/>
    <springProperty scope="context" name="MAX_FILE_SIZE" source="logging.file.max-size" defaultValue="10MB"/>
    <springProperty scope="context" name="TOTAL_SIZE_CAP" source="logging.file.total-size-cap" defaultValue="50MB"/>
    <springProperty scope="context" name="MAX_HISTORY" source="logging.file.max-history" defaultValue="30"/>

    <!-- 控制台输出  [%tid]是skywalking整合logback的traceid的占位符-->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
            <layout class="org.apache.skywalking.apm.toolkit.log.logback.v1.x.TraceIdPatternLogbackLayout">
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%tid] [%thread] %highlight(%-5level) %logger{36} - %msg%n</pattern>
            </layout>
        </encoder>
    </appender>

    <!-- 开发环境 -->
    <springProfile name="dev">
        <appender name="FILE_DEV" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_DIR}/${APP_NAME}-dev.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>${LOG_DIR}/archive/${APP_NAME}-dev-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
                <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
                <totalSizeCap>${TOTAL_SIZE_CAP}</totalSizeCap>
                <maxHistory>${MAX_HISTORY}</maxHistory>
            </rollingPolicy>
            <!-- ✅ 修改这里：使用 SkyWalking Layout -->
            <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
                <layout class="org.apache.skywalking.apm.toolkit.log.logback.v1.x.TraceIdPatternLogbackLayout">
                    <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%tid] [%thread] %-5level %logger{36} - %msg%n</pattern>
                </layout>
            </encoder>
        </appender>

        <!-- 根日志器 -->
        <root level="INFO">
            <appender-ref ref="FILE_DEV"/>
            <appender-ref ref="CONSOLE"/>
        </root>

        <!-- 特定包日志 -->
        <logger name="com.example.project.service" level="TRACE"/>
    </springProfile>

    <!-- 测试环境 -->
    <springProfile name="test">
        <appender name="FILE_TEST" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_DIR}/${APP_NAME}-test.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>${LOG_DIR}/archive/${APP_NAME}-test-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
                <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
                <totalSizeCap>${TOTAL_SIZE_CAP}</totalSizeCap>
                <maxHistory>${MAX_HISTORY}</maxHistory>
            </rollingPolicy>
            <!-- ✅ 修改这里：使用 SkyWalking Layout -->
            <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
                <layout class="org.apache.skywalking.apm.toolkit.log.logback.v1.x.TraceIdPatternLogbackLayout">
                    <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%tid] [%thread] %-5level %logger{36} - %msg%n</pattern>
                </layout>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="FILE_TEST"/>
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!-- 生产环境 -->
    <springProfile name="prod">
        <appender name="FILE_PROD" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_DIR}/${APP_NAME}.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>${LOG_DIR}/archive/${APP_NAME}-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
                <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
                <totalSizeCap>${TOTAL_SIZE_CAP}</totalSizeCap>
                <maxHistory>${MAX_HISTORY}</maxHistory>
            </rollingPolicy>
            <!-- ✅ 修改这里：使用 SkyWalking Layout -->
            <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
                <layout class="org.apache.skywalking.apm.toolkit.log.logback.v1.x.TraceIdPatternLogbackLayout">
                    <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%tid] [%thread] %-5level %logger{36} - %msg%n</pattern>
                </layout>
            </encoder>
        </appender>

        <!-- 异步包装 -->
        <appender name="ASYNC_FILE_PROD" class="ch.qos.logback.classic.AsyncAppender">
            <queueSize>512</queueSize>
            <discardingThreshold>0</discardingThreshold>
            <appender-ref ref="FILE_PROD"/>
        </appender>

        <root level="ERROR">
            <appender-ref ref="ASYNC_FILE_PROD"/>
            <!-- 生产环境也可以输出到控制台（容器环境需要） -->
            <appender-ref ref="CONSOLE"/>
        </root>

        <logger name="org.springframework" level="WARN"/>
        <logger name="org.hibernate" level="WARN"/>
    </springProfile>

</configuration>