<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.happy.mapper.SeataBalanceMapper">
  <resultMap id="BaseResultMap" type="com.happy.entity.SeataBalance">
    <id column="user_id" jdbcType="VARCHAR" property="userId" />
    <result column="username" jdbcType="VARCHAR" property="username" />
    <result column="available" jdbcType="DECIMAL" property="available" />
    <result column="frozen" jdbcType="DECIMAL" property="frozen" />
    <result column="version" jdbcType="INTEGER" property="version" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
  </resultMap>
  <sql id="Base_Column_List">
    user_id, username, available, frozen, version, create_time, update_time
  </sql>
  <select id="selectByPrimaryKey" parameterType="java.lang.String" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from seata_balance
    where user_id = #{userId,jdbcType=VARCHAR}
  </select>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.String">
    delete from seata_balance
    where user_id = #{userId,jdbcType=VARCHAR}
  </delete>
  <insert id="insert" parameterType="com.happy.entity.SeataBalance">
    insert into seata_balance (user_id, username, available, 
      frozen, version, create_time, 
      update_time)
    values (#{userId,jdbcType=VARCHAR}, #{username,jdbcType=VARCHAR}, #{available,jdbcType=DECIMAL}, 
      #{frozen,jdbcType=DECIMAL}, #{version,jdbcType=INTEGER}, #{createTime,jdbcType=TIMESTAMP}, 
      #{updateTime,jdbcType=TIMESTAMP})
  </insert>
  <insert id="insertSelective" parameterType="com.happy.entity.SeataBalance">
    insert into seata_balance
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="userId != null">
        user_id,
      </if>
      <if test="username != null">
        username,
      </if>
      <if test="available != null">
        available,
      </if>
      <if test="frozen != null">
        frozen,
      </if>
      <if test="version != null">
        version,
      </if>
      <if test="createTime != null">
        create_time,
      </if>
      <if test="updateTime != null">
        update_time,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="userId != null">
        #{userId,jdbcType=VARCHAR},
      </if>
      <if test="username != null">
        #{username,jdbcType=VARCHAR},
      </if>
      <if test="available != null">
        #{available,jdbcType=DECIMAL},
      </if>
      <if test="frozen != null">
        #{frozen,jdbcType=DECIMAL},
      </if>
      <if test="version != null">
        #{version,jdbcType=INTEGER},
      </if>
      <if test="createTime != null">
        #{createTime,jdbcType=TIMESTAMP},
      </if>
      <if test="updateTime != null">
        #{updateTime,jdbcType=TIMESTAMP},
      </if>
    </trim>
  </insert>
  <update id="updateByPrimaryKeySelective" parameterType="com.happy.entity.SeataBalance">
    update seata_balance
    <set>
      <if test="username != null">
        username = #{username,jdbcType=VARCHAR},
      </if>
      <if test="available != null">
        available = #{available,jdbcType=DECIMAL},
      </if>
      <if test="frozen != null">
        frozen = #{frozen,jdbcType=DECIMAL},
      </if>
      <if test="version != null">
        version = #{version,jdbcType=INTEGER},
      </if>
      <if test="createTime != null">
        create_time = #{createTime,jdbcType=TIMESTAMP},
      </if>
      <if test="updateTime != null">
        update_time = #{updateTime,jdbcType=TIMESTAMP},
      </if>
    </set>
    where user_id = #{userId,jdbcType=VARCHAR}
  </update>
  <update id="updateByPrimaryKey" parameterType="com.happy.entity.SeataBalance">
    update seata_balance
    set username = #{username,jdbcType=VARCHAR},
      available = #{available,jdbcType=DECIMAL},
      frozen = #{frozen,jdbcType=DECIMAL},
      version = #{version,jdbcType=INTEGER},
      create_time = #{createTime,jdbcType=TIMESTAMP},
      update_time = #{updateTime,jdbcType=TIMESTAMP}
    where user_id = #{userId,jdbcType=VARCHAR}
  </update>


  <!-- Try：冻结金额 -->
  <!-- 条件：可用余额 >= amount，且使用 version 实现乐观锁 -->
  <update id="freezeBalance">
    UPDATE seata_balance
    SET
      available = available - #{amount},
      frozen = frozen + #{amount},
      update_time = NOW()
    WHERE
      user_id = #{userId}
      AND available >= #{amount}
  </update>

  <!-- Confirm：确认扣款 -->
  <!-- 条件：冻结金额 >= amount -->
  <update id="confirmDeduct">
    UPDATE seata_balance
    SET
      frozen = frozen - #{amount},
      update_time = NOW()
    WHERE
      user_id = #{userId}
      AND frozen >= #{amount}
  </update>

  <!-- Cancel：释放冻结金额 -->
  <!-- 条件：冻结金额 >= amount -->
  <update id="cancelFreeze">
    UPDATE seata_balance
    SET
      frozen = frozen - #{amount},
      available = available + #{amount},
      update_time = NOW()
    WHERE
      user_id = #{userId}
      AND frozen >= #{amount}
  </update>


  <!-- seata的at模式：直接扣款 -->
  <!-- 条件：可用金额 >= amount -->
  <update id="deductBalance">
    UPDATE seata_balance
    SET
      available = available - #{amount},
      update_time = NOW()
    WHERE
      user_id = #{userId}
  </update>



</mapper>